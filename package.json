{
    "name": "mini_project_membuat_perhitungan_bangun_datar",
    "version": "1.0.0",
    "description": "ini merupakan project menghitung luas bangun datar, dimana kita dapat memasukkan input kemudian kalkulasi hitungannya",
    "scripts": {
        "start": "parcel ./*.html",
        "prebuild": "rm -rf dist/*",
        "build": "parcel build ./*.html --public-url ./",
        "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "gh-pages": "^3.2.3",
        "parcel": "^2.0.1"
    }
}
